let fila = document.getElementById("fila-cards");//obtengo el contenedor fila 
let contCard = document.getElementById("contenedor-card"); // devuelve el primer contenedor
let card = document.getElementsByClassName("card"); //devuelve un array con las cards
console.log(card);
